# ===== ARKA SOLID PASS — .env =====
# Backend (BFF) base URL
API_BASE_URL=http://localhost:8080
# Optional API key header for BFF (X-API-Key)
API_KEY=
# UI base URL (nginx 8082 or vite 5173)
UI_BASE_URL=http://localhost:8082

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=arka
DB_USER=arka
DB_PASSWORD=arka

# Default client/project keys for tests
TEST_CLIENT=ACME
TEST_PROJECT_KEY=ACME-CORE

# Controls
EXPECT_NONZERO_FLOWS=1
EXPECT_NONZERO_PROJECTS=0
ALLOW_WRITE=0           # 1 to allow POST thread/message test
SSE_PATHS=/api/notify/stream,/api/notifications/stream,/api/notifications/sse,/api/events/stream,/api/hp/stream
TIMEOUT_SECONDS=8
